on: issue_comment
name: Run Stale Bot on Issue Comments
jobs:
  build:
    name: stale
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - run: npm install
      - run: npm build
      - name: stale
        uses: LekoArts/stale@experiment
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          stale-issue-message: 'message blabla'
          exempt-issue-labels: |
            not stale
            important
